% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bm_kable.R
\name{bm_kable}
\alias{bm_kable}
\title{Format tables for Quarto rendering via knitr::kable}
\usage{
bm_kable(
  x,
  lang = c("eng", "ger")[1],
  pvalform = "p.value",
  digits = "round_smart",
  add_abbrev_footnote = TRUE,
  align = NULL,
  as_kable = TRUE,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{x}{Table to be formatted (data.frame, tibble, anova, etc.)}

\item{lang}{Language for column names. Either \code{"eng"} (default) or \code{"ger"}.}

\item{pvalform}{Names of columns formatted via \code{\link[=format_p]{format_p()}}. Default is
\code{"p.value"}. Set to \code{NULL} to skip p-value formatting.}

\item{digits}{Number of digits for rounding. Default \code{"round_smart"} applies
\code{\link[=round_smart]{round_smart()}} per column. Can be a numeric value for fixed rounding.}

\item{add_abbrev_footnote}{If \code{TRUE} (default), stores an abbreviation
footnote as an attribute. Retrieve with \code{\link[=bm_footnote]{bm_footnote()}}.}

\item{align}{Column alignment. \code{NULL} (default) auto-detects: numeric
columns right-aligned, others left-aligned. Can be a character vector
(e.g., \code{c("l", "r", "r")}).}

\item{as_kable}{If \code{TRUE} (default), returns a \code{knitr_kable} object.
If \code{FALSE}, returns the processed tibble.}

\item{verbose}{If \code{TRUE}, prints transformation details. Default \code{FALSE}.}

\item{...}{Other arguments passed to \code{\link[=round_smart]{round_smart()}}.}
}
\value{
A \code{knitr_kable} object (pipe format) when \code{as_kable = TRUE},
or a tibble when \code{as_kable = FALSE}. The kable object may have a
\code{"footnote"} attribute containing abbreviation explanations.
}
\description{
Formats statistical tables (ANOVA, emmeans, etc.) for Quarto documents using
\code{knitr::kable()} with pipe format. Produces clean output compatible with
Quarto cross-references (\verb{@tbl-*}) across HTML, PDF (Typst), and DOCX.

Uses the same column unification, formatting, and renaming pipeline as
\code{\link[=docx_tab]{docx_tab()}}, but outputs a \code{knitr_kable} object instead of a flextable.
Visual formatting (borders, font size, padding) is handled by the output
template (e.g., \code{reference.docx} Table style), not inline.
}
\examples{
library(BioMathR)

# Basic usage with ANOVA table
anova <- anova(lm(weight ~ group, data = PlantGrowth))
bm_kable(anova)
bm_kable(anova, lang = "ger")

# Return tibble instead of kable
bm_kable(anova, as_kable = FALSE)

# Footnote retrieval
tab <- bm_kable(anova)
bm_footnote(tab)

# Custom alignment
bm_kable(anova, align = c("l", "r", "r", "r", "r", "r"))
}
\seealso{
\code{\link[=bm_footnote]{bm_footnote()}} to print the footnote, \code{\link[=docx_tab]{docx_tab()}} for
flextable-based output (non-Quarto workflows).
}
